#set the size of the dimension
set_dim = (64,64)
#set the max proposal inference
maximum_prop_inference = 200
# define model's path and the label's path
modpath= "aircraft_detector.h5"
labpath = "label.pickle"
# minimum probability to accept the result as a true positive
Proba_minimum = 0.5
